// Directory data for ./content — applies to every canonical Markdown file.
// Kept out of the files' front matter so the served .md carries only
// meaningful metadata (title, description), not build plumbing.
module.exports = {
  layout: "base.njk",
  tags: "page",
};
